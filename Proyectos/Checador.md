---
tipo: proyecto
nombre: Checador
tags: [proyecto, checador, asistencia]
estado: activo
actualizado: 2026-07-17
---

# 🕒 Checador

Nodo principal del área Checador: control de asistencia / registro de entradas y salidas.

## Alcance / temas
- [pendiente] Describir qué cubre (registro de checadas, horarios, reportes de asistencia, etc.).

## Conversaciones relacionadas
- *Se enlazan al archivar sesiones; si no hay, conserva este placeholder.*

## Datos clave
> Datos duros del área (solo reales; usa [pendiente] si falta o no aplica).
- **Stack**: [pendiente]
- **Arquitectura general**: [pendiente]
- **Backend**: [pendiente]
- **Frontend**: [pendiente]
- **Base de datos**: [pendiente]
- **Componentes reutilizables**: [pendiente]
- **API / endpoints**: [pendiente]
- **Despliegue / infra**: [pendiente]
- **Seguridad**: [pendiente]

## Datos / conexiones con otros proyectos
> Info que se cruza con otras áreas. Usa [[enlaces]].
- Autenticación compartida → [[Usuarios AUD (autenticación)]].
- [pendiente] Definir conexiones con otros nodos (p. ej. nóminas en [[Gestión (Tesorería)]]).

## Pendientes
- [ ] [pendiente] Poblar el nodo con datos reales del proyecto.

## Enlaces
- [[00 Índice de Memoria]]

## Historial de commits (auto — la actualiza el git hook; no editar a mano)
> Una entrada por commit: fecha/hora, hash, mensaje y archivos.
<!-- COMMITS:START -->
<!-- COMMITS:END -->

## Memoria del repo (auto — la actualiza el git hook; no editar a mano)
> Sincronizado desde `docs/MEMORIA.md` en cada commit.
<!-- MEMORIA:START -->
---
tipo: proyecto
nombre: Checador
tags: [proyecto, checador]
estado: activo
actualizado: 2026-07-17
---
# ⏰ Checador
Nodo principal del área Checador: sistema de asistencia con reconocimiento facial, validación GPS y lista blanca de Wi-Fi, 100% web (PWA) sobre Docker.
## Alcance / temas
- Registro de asistencia entrada/salida por reconocimiento facial 1:N (dlib ResNet 128-d).
- Validación de geocerca (haversine vs `radius_m`) y lista blanca de Wi-Fi por sucursal.
- App empleado (login por PIN, home, check-in, historial) + panel admin (empleados, sucursales, Wi-Fi, equipo, reportes).
- Modo quiosco con JWT propio (`/kiosk`) y modo público sin login (`/probar`).
- Detección de rostro en el navegador con MediaPipe Face Mesh; el embedding 128-d se calcula en el backend.
## Conversaciones relacionadas
- *Se enlazan al archivar sesiones.*
## Datos clave
- **Stack** — Front React 18 + Vite; API FastAPI (Python 3.12); BD Postgres 16 + `pgvector`; orquestado con Docker Compose. Reconocimiento facial: MediaPipe Face Mesh (navegador, 468 landmarks) + `face_recognition`/dlib 128-d (servidor).
- **Arquitectura general** — 3 servicios: `web` (React→build Vite→nginx: sirve estáticos y proxya `/api`), `api` (FastAPI, embeddings dlib) y Postgres (en el host, fuera de compose). Auth por JWT (empleado y quiosco). El navegador detecta el rostro con MediaPipe y sube un JPEG; el backend extrae el vector 128-d y lo compara contra `face_embeddings`.
- **Backend** — `api/app/main.py` monta routers con prefijo `/api`: `auth`, `enroll`, `checkin`, `admin`, `kiosk`, `me`, `test`. SQLAlchemy 2 + asyncpg; bcrypt (PIN); PyJWT; geocerca haversine (`app/geo.py`); validación de SSID. Migración idempotente `db/migrate.sql` al arrancar si `AUTO_MIGRATE=true` (`api/seed.py`).
- **Frontend** — `web/src/` React + react-router-dom 6. Pantallas empleado (Login, Home, CheckIn, Enroll, History) y admin (Today, Employees, EnrollEmployee, Locations, Wifi, Equipment, Reports) + Kiosk/KioskSetup/Probar. MediaPipe cargado por CDN en `index.html`. Mapa Leaflet, gráficas Recharts, export xlsx/jspdf.
- **Base de datos** — Postgres 16 + `pgvector`. Esquema en `db/init.sql` (primera vez), `db/migrate.sql` (idempotente) y `db/equipment.sql`. Tablas: `locations`, `allowed_ssids`, `employees`, `employee_locations`, `contact`, `departments`, `positions`, `face_embeddings` (`vector(128)`, índice HNSW l2), `attendance`, `equipment`, `incidencias` (+ `monday_item_id TEXT UNIQUE`: id del item de Monday para upsert idempotente; NULL = capturada en el panel). Columna `employees.work_days SMALLINT[]` (ISO 1=Lun…7=Dom; NULL = default global L-V, definido en `app/report_person.py`).
- **Componentes reutilizables** — `components/admin/`: Modal, ConfirmModal, DataTable, Stepper, KpiCard, Avatar, CatalogSelect, EnrollFaceModal, Toaster (+ `lib/toast.js`, pub/sub de toasts). `components/`: Icon (set SVG lucide-style), FaceOverlay, Select, DatePicker/TimeField, ColorPicker, KioskHeaderCard.
- **API/endpoints** — (prefijo `/api`) `auth/login`; `checkin`; `enroll/face`; `me` (+`/attendance`,`/stats`); `kiosk/{me,identify,identify-multi}`; `admin/*` (CRUD employees/departments/positions/equipment/locations/ssids/**incidencias**, `today*`, `reports/{attendance,retardos,failures,**asistencia-por-persona**}`, `stats`, `kiosk-token`, `wifi`, `employees/{id}/face`, `employees/{id}/reset-pin`); `test/*` (público); `health`; `webhooks/monday` (inbound de Monday, sin auth de sesión: secreto por query `?s=` o header).
- **Lógica pura testeable** — `app/shift.py` (ventana de salida) y `app/report_person.py` (estado de cada día del reporte por persona). Sin BD ni FastAPI; sus checks son `api/test_exit_window.py` y `api/test_report_person.py`, con asserts y sin framework (no hay pytest): `cd api && python test_report_person.py` → `ok`. **Regla del repo**: `attendance.ts` es TIMESTAMPTZ en UTC y los horarios/permisos se capturan en local MX, así que toda comparación convierte con `astimezone(MX_TZ)` antes.
- **Despliegue/infra** — Dev: `docker-compose.yml`. Prod: `docker-compose.server.yml` (imagen `web` nginx `127.0.0.1:3105→80`; imagen `api` expone `8000`; el front se compila DENTRO de la imagen vía `web/Dockerfile.prod`). Deploy remoto desde Windows: `deploy-remoto.ps1` (alias SSH `n8n-tickets`, dir `~/checador`) → `deploy-server.sh` (`docker compose build && up -d`). Prod: https://n8n.datazentrika.com/checador/ (subpath: `VITE_BASE_PATH=/checador/`, `VITE_API_URL=/checador`). **Deploy quirúrgico**: el código va horneado en la imagen (solo `./db` va montado), así que hay que `build` tras `scp`; y sube SIEMPRE los módulos nuevos que importa el código + `db/migrate.sql` junto al archivo tocado — un import faltante tumba `api` al arrancar (**502**) y un `migrate.sql` viejo deja columnas sin crear (**500**). Lo seguro: `scp -r api/app` + `scp db/migrate.sql` antes del build (`migrate.sql` es idempotente).
- **Seguridad** — `/api/test/identify` está PÚBLICO sin auth (riesgo: cualquiera con foto de un empleado enrolado puede fichar; documentado en el README). Secretos en `api/.env` / `api/.env.server` (`JWT_SECRET`, `DB_PASSWORD`, `DATABASE_URL`, `FACE_THRESHOLD`) — no versionar; no leídos aquí. Privacidad: solo se guarda el vector 128-d, nunca la imagen. HTTPS obligatorio para cámara/GPS.
## Datos / conexiones con otros proyectos
- **Infra n8n (datazentrika)** — comparte host y el nginx del server; la app vive bajo el subpath `/checador` dentro del `server{}` de `n8n.datazentrika.com`; el alias SSH de deploy es `n8n-tickets`.
- **Postgres compartido** — en server usa `DB_SCHEMA=checador` sobre la BD Postgres del host (`host.docker.internal`), compartida con ese stack.
- **Monday (externo)** — board **"Permisos"** (863 items) es la puerta de solicitudes. La aprobación se marca por **GRUPO**, no por columna (la columna Estado está vacía): grupos `emailed_elementos`=Recibidos, **`grupo_nuevo`=Autorizados** (disparador), `grupo_nuevo36581`=Rechazados, `grupo_nuevo81661`=2023. Automatización "item se mueve a grupo Autorizados" dispara `POST /api/webhooks/monday?s=<secreto>`; el checador lee nombre+grupo+columnas por GraphQL (`MONDAY_API_TOKEN`), valida el grupo y crea la incidencia. **Match del empleado POR NOMBRE normalizado** (checador tiene los 62 empleados SIN correo, 0/62; Monday no tiene emp_code) — `monday_map.match_employee`, estricto: 0 o 2+ coincidencias → se salta (no adivina). Board multi-empresa (teamrh.mx, mfbcp.com.mx, impxs.com.mx, acuarioempresarial.com…). Columnas: tipo=`selecci_n__nica6`, fecha=`date` (un día), hora=`selecci_n__nica3` ("04:30 pm"), motivo=`texto3`, estado=`estado3`. Config (IDs + `tipo_map`) en `app/routers/webhooks.py`.
## Pendientes
- [ ] **Monday: configuración/backfill pendiente.** Código listo (mapeo, tipo `permiso`, match por nombre ~90%, guard por grupo). Falta: (a) env en `.env.server` (`MONDAY_WEBHOOK_SECRET`, `MONDAY_API_TOKEN`); (b) registrar la automatización en Monday ("item se mueve a grupo *Permisos Autorizados*" → `POST /api/webhooks/monday?s=<secreto>`); (c) decidir **backfill** del histórico (863 items; script que pagine el grupo Autorizados e inserte) vs solo nuevas; (d) deploy. Match validado contra el board real (500 muestras): **90% (454/500)**; los ~10% restantes son gente que NO está en checador (Leticia Villagómez, Fabiola Sánchez, Carlos Estrada, Clara Zavala, Yedtsa Luna) o apodos duros (Rossy→Rosa, "Ilse Hdez"). Para subir: alias en `employees` o diccionario de apodos. Opcional a futuro: empujar resumen de vuelta a Monday.
- [ ] Seguridad: `/api/test/identify` expuesto sin auth. Mitigar: quitar el link del login, exigir token de quiosco, o filtrar por IP en nginx.
- [ ] Quitar el HUD/medidor temporal de recursos en `web/src/screens/KioskClassic.jsx` (marcado `ponytail:`) tras calibrar el modo ligero en la tablet.
- [ ] Reconocimiento con cubrebocas limitado (dlib) — bajar `FACE_THRESHOLD`, re-enrolar con cubrebocas, o migrar a ArcFace 512-d (requiere migración de BD).
- [ ] Bundle web >500 kB (warning de Vite en build) — evaluar code-splitting.
- [ ] Bug: la paridad in/out en `kiosk.py` cuenta los eventos "del día" con `ts::date = now()::date` en **UTC**, no en hora de México. Después de las 18:00 MX (00:00 UTC) el contador se reinicia y el siguiente escaneo se registra como ENTRADA. Afecta turnos que salen tarde. Fix: `(ts AT TIME ZONE 'America/Mexico_City')::date` como ya hace `admin.py`.
- [ ] **Salida anticipada**: `permiso_salida_temprano` hoy se registra pero NO altera el estado del día, porque el sistema no tiene el concepto de "salida anticipada" (nunca lo tuvo). Si se quiere detectar, hay que comparar `hora_salida` contra `shift_end` y que el permiso la excuse.
- [ ] Verificar el **bug de TZ del retardo en el reporte plano** (`admin.py`, cálculo de `estado_calc`): hace `entrada.replace(hour=ssh)` sobre un TIMESTAMPTZ en UTC encajándole una hora local MX ⇒ compararía 15:02 UTC vs 09:00 UTC y marcaría Retardo a todos. El reporte por persona ya lo hace bien (`report_person.py` convierte a MX_TZ). Confirmar con datos reales y arreglar.
- [ ] **Reportes automatizados** (mensual, que el admin elija cuándo, envío por correo). Diseño evaluado 2026-07-16: el export ya existe (`/api/admin/reports/{attendance,retardos,failures}?fmt=xlsx|csv`, openpyxl); faltan scheduler + email. **No hay atajo vía n8n**: pese al nombre `n8n-tickets` / `n8n.datazentrika.com`, el server no tiene contenedor n8n ni de correo (16 contenedores, ninguno). Propuesta sin dependencias nuevas: tabla `report_schedules` (report, fmt, freq, day_of_month, hour, recipients, location_id, active, next_run_at) + tick horario `asyncio` (`WHERE active AND next_run_at <= now()`, así un restart se pone al corriente) + `smtplib`/`EmailMessage` (stdlib). Requiere refactor: separar "construir filas" y "construir xlsx bytes" de `_xlsx_response` (hoy solo devuelve `StreamingResponse`). Riesgos: deliverability (usar relay SMTP, no enviar directo del VPS) y evaluar horarios en `America/Mexico_City`.
## Registro de cambios
> Append-only, lo más reciente arriba: <fecha> — qué cambió · por qué · archivos.
- 2026-07-17 — **Flujo propio de permisos (form + aprobación por jefe directo), alternativa a Monday.** El empleado solicita desde la app (con su PIN) y su **jefe directo** aprueba/rechaza en una bandeja; al aprobar cuenta en el reporte. (a) Migración: `employees.manager_id` (jefe directo), `incidencias.estado` (`pendiente`/`autorizado`/`rechazado`, **default `autorizado`** para que lo existente —Monday, panel— siga contando), `resuelto_por`/`resuelto_at`. (b) `me.py`: `POST/GET /api/me/permisos` (solicita → nace `pendiente`), `GET /api/me/aprobaciones` + `POST /api/me/aprobaciones/{id}` (solo el jefe directo del solicitante, solo si sigue pendiente), y badges en `/api/me` (`permisos_pendientes`, `es_jefe`, `aprobaciones_pendientes`). Reusa `INCIDENCIA_TIPOS`/`_TIPOS_CON_HORA` de admin. (c) **Reporte por persona: el fetch de incidencias filtra `estado='autorizado'`** (pendiente/rechazado no justifican). (d) Admin: `manager_id` en API de empleados + selector "Jefe directo" en el form. (e) Front: pantallas `Permisos.jsx` (solicitar + mis solicitudes) y `Aprobaciones.jsx` (bandeja del jefe) + botones/badges en Home. · el usuario quiere manejar todo el proceso desde checador, sin depender de Monday ni del matching por nombre (el permiso nace pegado al employee_id real). Fase 1 sin correo (bandeja in-app); convive con Monday. · Archivos: db/migrate.sql, api/app/routers/{me,admin}.py, web/src/screens/{Permisos,Aprobaciones,Home}.jsx (Permisos/Aprobaciones nuevos), web/src/App.jsx, web/src/screens/admin/Employees.jsx.
- 2026-07-17 — **Integración Monday → checador (inbound), board "Permisos".** Las solicitudes AUTORIZADAS entran como incidencias y aparecen en el reporte por persona, sin doble captura. `POST /api/webhooks/monday`: eco del `challenge`; secreto por query `?s=` (el webhook nativo de Monday no manda headers) o header; el webhook solo trae `pulseId`, así que se lee nombre+columnas por GraphQL (`MONDAY_API_TOKEN`); el router valida que el item esté en el grupo Autorizados (`grupo_nuevo`) y `map_item` (puro, `test_monday_map.py`) mapea tipo/fecha/hora/nota, con **upsert idempotente por `monday_item_id`** (`TEXT UNIQUE`). **Match del empleado POR NOMBRE normalizado** (`match_employee`: sin acentos/particulas; casa exacto, apodo=prefijo casi completo — eli→elizabeth, gaby→gabriela, laxo solo en tokens cortos para no cruzar rodrigo~rodriguez — e iniciales sueltas o pegadas "TR"→t,r; 0 o 2+ → skip, no adivina) porque checador tiene 0/62 correos y Monday no trae emp_code; validado 90% (454/500) contra el board real; sin match → 200 `skipped` + log. Mapeo real: tipo=`selecci_n__nica6` (Home Office→home_office, Salir antes→salida_temprano, Llegaré tarde→entrada_tarde, Asunto personal/Otros→**permiso**), fecha=`date` (un día), hora=`selecci_n__nica3` ("04:30 pm", parser am/pm), motivo=`texto3`. `created_by` NULL. · Monday es la puerta de solicitudes; se pidió reflejarlas. · Archivos: api/app/monday_map.py (nuevo), api/test_monday_map.py (nuevo), api/app/routers/webhooks.py (nuevo), api/app/main.py, db/migrate.sql.
- 2026-07-17 — Nuevo tipo de incidencia **`permiso`** (asunto personal, día o medio día) que en el reporte cuenta como **justificado** (no falta), como vacaciones/incapacidad. Añadido a `PERMISO`/`_JUSTIFICADO` en `report_person.py` (+ test), al CHECK de `incidencias.tipo` (CREATE + `ALTER … DROP/ADD CONSTRAINT` idempotente para BD existente), a `INCIDENCIA_TIPOS`/`_ESTADO_LABEL`/`_incidencia_desc` (admin.py), y al dropdown de la pantalla Incidencias + `ESTADO_LABEL` de Reportes. · el board de Monday tiene "Asunto personal Día completo/Medio DÍA/Otros" que no calzaban en los 5 tipos previos. · Archivos: api/app/report_person.py, api/test_report_person.py, api/app/routers/admin.py, db/migrate.sql, web/src/screens/admin/{Incidencias,Reports}.jsx.
- 2026-07-17 — Recepción ahora puede **generar reportes**, acotada a su(s) sucursal(es) igual que `can_reports`. `_require_reports` pasó de (admin OR can_reports) a (admin OR is_reception OR can_reports); el acotamiento ya usaba `_reception_branch_ids`, así que sin sucursal asignada da 403 claro. Front: ruta y ítem de nav "Reportes" visibles para recepción. · lo pidió el usuario. · Archivos: api/app/routers/admin.py, web/src/screens/Admin.jsx, web/src/components/admin/AdminLayout.jsx.
- 2026-07-16 — Nuevo permiso `can_reports` ("solo reportes", acotado a sucursal como recepción, sin ser admin). Columna `employees.can_reports BOOL`; el gate de `reports/attendance` y `reports/asistencia-por-persona` pasó de `_require_admin` a `_require_reports` (admin OR can_reports) + acotamiento por sucursal en `emp_where` y `att_where` vía `reports_scope.resolve_scope` (pura, testeable). Recepción NO puede otorgar el permiso. Los GET de catálogo que la pantalla de Reportes necesita (`employees`, `locations`, `departments`, `positions`) pasaron a `_require_panel` (admin/recepción/can_reports); `employees` y `locations` se acotan a la sucursal del usuario vía `_branch_scoped` (unifica el viejo `if _is_reception`), y a un can_reports puro se le quita `pin_plain` de la respuesta (no gestiona empleados). Front: `sessionFlags().canReports` (+ claim `rep` en JWT), rutas/menú del panel muestran solo Reportes para ese rol, toggle "Puede ver reportes" en Empleados, Home/Login lo propagan. `reports/retardos` y `reports/failures` siguen solo-admin (la UI no los expone). · El usuario quería dar reportes a 3 personas sin hacerlas admin completo, igual que recepción se limita a su sucursal. · Archivos: db/migrate.sql, api/app/auth.py, api/app/routers/auth.py, api/app/routers/admin.py, api/app/reports_scope.py (nuevo), api/test_reports_scope.py (nuevo), web/src/lib/session.js, web/src/screens/{Admin,Home,Login}.jsx, web/src/components/admin/AdminLayout.jsx, web/src/screens/admin/Employees.jsx.
- 2026-07-17 — Reporte por persona (xlsx + acordeón): fuera el carácter `·` (título, puesto/depto ahora con `/`, resumen separado por espacios, estado compuesto → "A tiempo sin salida") y **retardos ocultos por ahora** (se colapsan a "A tiempo" en Estado; se quitó el conteo del resumen y el chip). `summarize()` sigue calculando retardos, solo no se muestran (invariante intacto), así que revertirlo es 1 línea. · el `·` se veía sucio y los retardos aún no son confiables (bug de TZ pendiente en el reporte plano). · Archivos: api/app/routers/admin.py, web/src/screens/admin/Reports.jsx.
- 2026-07-17 — Alias también al iniciar sesión (el quiosco ya lo usaba). El login (`auth.py`) y `/api/me` ahora devuelven `alias`; `Login.jsx` lo guarda en el profileStore; el saludo de Home y del panel admin usa `alias || primer nombre`. · consistencia: la persona se saluda con el nombre que prefiere en todos lados, no solo en el quiosco. Ojo: `/api/me` debía incluir `alias` porque `refreshProfile()` hace `{...prev, ...fresh}` al montar. · Archivos: api/app/routers/auth.py, api/app/routers/me.py, web/src/screens/Login.jsx, web/src/screens/Home.jsx, web/src/components/admin/AdminLayout.jsx.
- 2026-07-16 — **Reporte de asistencia por persona + incidencias.** (a) Nueva columna `employees.work_days SMALLINT[]` (NULL = default global L-V) y tabla `incidencias` (vacaciones, incapacidad, home_office, permiso_entrada_tarde, permiso_salida_temprano; rango de fechas + hora solo en permisos). (b) El `CROSS JOIN` de `include_faltas` del reporte plano ahora filtra por día laboral: **antes un domingo contaba como Falta para todos**. (c) Módulo puro `app/report_person.py` + check `api/test_report_person.py` (patrón de `shift.py`/`test_exit_window.py`): home office = asistió, vacaciones/incapacidad = justificado, permiso de entrada tarde mueve la hora contra la que se mide el retardo, y compara en `MX_TZ` (no en UTC). Invariante testeado: `asistidos + justificados + faltas == dias_a_laborar`. (d) CRUD `/api/admin/incidencias` + pantalla Incidencias. (e) `GET /api/admin/reports/asistencia-por-persona?fmt=json|xlsx` con acordeón por empleado y export xlsx por bloques. · Se pedía un reporte por persona con días asistidos/no asistidos según los días que le tocaba ir; ninguno de esos dos datos existía en BD. · Archivos: db/migrate.sql, api/app/report_person.py (nuevo), api/test_report_person.py (nuevo), api/app/routers/admin.py, web/src/screens/admin/Incidencias.jsx (nuevo), web/src/screens/admin/{Employees,Reports}.jsx, web/src/screens/Admin.jsx, web/src/components/admin/AdminLayout.jsx, web/src/styles.css, docs/superpowers/specs/2026-07-16-reporte-asistencia-por-persona-design.md, docs/superpowers/plans/2026-07-16-reporte-asistencia-por-persona.md.
- 2026-07-16 — Reportes: se quitaron las columnas "Ubicación de entrada/salida" (hacían geocodificación inversa por fila, con red) y sus helpers `nearestBranch`/`haversineM`/`BranchOrGeoCell`; se queda "Sucursal". Se **reactivaron los botones de descarga** (estaban comentados en el JSX, por eso no se podía exportar nada) y se agregó PDF con `jspdf-autotable` (dep. nueva, se importa con `import()` dinámico para no engordar el bundle inicial). Nombre de archivo legible: "reporte uptown abril 2026" — solo se nombra por mes si el rango es el mes COMPLETO, si no usa el rango. El XLSX ahora lleva título/subtítulo, bandeado, autofiltro y ajustes de impresión. · Archivos: web/src/screens/admin/Reports.jsx, web/src/screens/admin/reportFilename.test.mjs (nuevo), web/package.json, api/app/routers/admin.py.
- 2026-07-16 — Campo `alias` en empleados (nombre con el que prefieren que los identifiquen) + pistas del quiosco más grandes con icono. El quiosco saluda "Registrado, \<alias\>" con fallback al primer nombre; vacío/espacios se guarda como NULL. El PATCH de empleados ahora llama `invalidate_face_cache()`: el cache lleva copia de name/alias/role, sin esto el quiosco saludaría con el alias viejo hasta re-enrolar. · Archivos: db/migrate.sql, api/app/routers/admin.py, api/app/routers/kiosk.py, api/app/face_cache.py, web/src/screens/admin/Employees.jsx, web/src/screens/KioskClassic.jsx, web/src/styles.css.
- 2026-07-16 — Ventana de salida + "Registrado ✓": el quiosco ahora usa `shift_end_hour/minute` (antes se guardaban pero NUNCA se usaban); desde 10 min antes de la hora de salida en adelante cualquier escaneo se registra como SALIDA, sin importar la paridad de eventos. El re-escaneo (duplicate) ya no muestra "Ya estabas registrado" sino "Registrado ✓" en verde. · La paridad sola registraba ENTRADA a la hora de irse cuando la persona salía a comer (4 eventos); y el mensaje de duplicado confundía. La ventana se evalúa en `America/Mexico_City` porque `attendance.ts` es TIMESTAMPTZ en UTC. · Archivos: api/app/shift.py (nuevo), api/app/routers/kiosk.py, api/test_exit_window.py (nuevo), web/src/screens/KioskClassic.jsx, web/src/components/FaceChips.jsx, web/src/styles.css.
- 2026-07-16 — Rollback del neumorfismo a "flat pro" (superficie blanca sobre fondo gris, sombra suave de una dirección, hovers sutiles, se conservan los radios) + iconos por tipo de equipo en el inventario. · El neumorfismo dejaba todo del mismo color; se prefirió blanco/plano con profundidad sutil. · Archivos: web/src/styles.css, web/src/components/Icon.jsx, web/src/screens/admin/Equipment.jsx.
- 2026-07-06 — Creación de `docs/MEMORIA.md` (contexto recuperable, Datos clave por sub-temas) + regla "Memoria del proyecto" en `CLAUDE.md`. · sincronizable a bóveda de Obsidian. · Archivos: docs/MEMORIA.md, CLAUDE.md.
- 2026-07-06 — Mejoras UX (feedback + visual): toasts en admin + spinner de guardado; contraste AA en tokens `--warn`/`--ok`; tokens de sombra y "cards premium"; fix `RESULT_HOLD_MS` 2s→4s; errores específicos de cámara (kiosko) y red/credenciales (login). · legibilidad y feedback de acciones. · Archivos: web/src/lib/toast.js, web/src/components/admin/Toaster.jsx, web/src/components/admin/AdminLayout.jsx, web/src/screens/admin/{Employees,Equipment,Locations,Wifi}.jsx, web/src/screens/{Login,KioskClassic}.jsx, web/src/styles.css.
<!-- MEMORIA:END -->

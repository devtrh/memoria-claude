---
tipo: endpoint
proyecto: POS CRM
recurso: clientes
metodo: POST
path: "/api/clientes/:id/documentos"
auth: jwt
escribe: true
tags: [endpoint, pos-crm]
---

`POST /api/clientes/:id/documentos` — recurso **clientes**, auth `jwt`.

[[POS CRM]]

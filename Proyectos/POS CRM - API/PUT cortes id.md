---
tipo: endpoint
proyecto: POS CRM
recurso: cortes
metodo: PUT
path: "/api/cortes/:id"
auth: jwt
escribe: true
tags: [endpoint, pos-crm]
---

`PUT /api/cortes/:id` — recurso **cortes**, auth `jwt`.

[[POS CRM]]

---
tipo: endpoint
proyecto: POS CRM
recurso: webhook-atena
metodo: POST
path: "/api/webhook/atena"
auth: webhook-secret
escribe: true
tags: [endpoint, pos-crm]
---

`POST /api/webhook/atena` — recurso **webhook-atena**, auth `webhook-secret`.

[[POS CRM]]
